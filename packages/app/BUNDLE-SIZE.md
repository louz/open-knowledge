# Bundle-size limits — rationale and history

The `size-limit` entries in `package.json` are hand-maintained: `size-limit` ships
no auto-update or ratchet, so each limit is measured and then edited by hand. This
file records *why* each limit moved, so that reasoning survives without living on a
single line of `package.json` that every branch has to append to.

The limits themselves stay in `package.json` — they are read by the
`open-knowledge / size` CI job. Only the narrative moved here.

## Open follow-ups

TODO: check why after installing @pierre/trees has exceeded by 55 kB.

- PropertyPanel pulled date-fns + react-day-picker into the eager bundle (~24 kB) — follow-up should lazy-load DateWidget so the calendar/date-fns chunk only loads on first date-property edit.
- @dnd-kit adds ~15 kB to the eager bundle (PropertyPanel imports it at top level) — follow-up should lazy-load PropertyPanel itself so dnd-kit + the widget zoo only load when a doc has frontmatter.
- terminal multi-session added ~2.65 kB to the eager bundle (TerminalTabStrip + the Radix Tabs primitive) — follow-up should lazy-load TerminalDock, which is desktop-only (gated on terminalBridge) and ships as dead weight in the web bundle.
- the bottom Ask AI composer (BottomComposer + AgentSplitButton) sits in the eager bundle though it is desktop-only (gated on isDesktop) — follow-up should lazy-load BottomComposer so it stops shipping as dead weight in the web bundle (would reclaim ~4 kB).

## Limit-change history

Limit raised 410->450 kB after merging main + the skill/template editor feature; the skill-specific UI (ManagedArtifactProperties, SkillEditorActions) is already lazy-loaded, so the residual is the pre-existing PropertyPanel tech-debt above — lazy-loading PropertyPanel should bring this back under 410.

Main CSS limit raised 49->52 kB after merging main + the skills-as-content UI — Tailwind utility accretion across many new surfaces, no single bloat source and no lazy-load lever for global CSS.

Limit raised 450->454 kB after merging main + the worktree selector feature (base-branch picker with local/remote search, flyout keyboard nav, project-navigator icon tiles); ~2 kB of this bump is concurrent main growth (incl. terminal-chat-controls #2350) merged in during finalization, and the PropertyPanel/TerminalDock/BottomComposer lazy-load levers above remain the reclamation path to bring this back down.

Combined-chunks limit raised 3.05->3.06 MB after adding the standalone Mermaid doc editor (MermaidDocEditor: diagram + editable Y.Text source with mermaid highlighting) — it is lazy-loaded (main bundle unaffected, still under 460 kB) so the growth is a new lazy chunk, not eager weight; the mermaid renderer + codemirror-lang-mermaid grammar were already in the chunk graph (prop-editor + edit-modal), so the delta is the doc-editor glue only.

Raised again 3.06->3.07 MB when rebasing onto main during finalization — ~2 kB is concurrent main growth (main app bundle grew 456.8->458.1 kB from unrelated merges), not the mermaid editor.

Main app bundle limit raised 460→462 kB after the mermaid-doc Ask-AI-composer-clearing fix (78ff0e5) pushed eager weight to 460.33 kB (333 B over) — the BottomComposer lazy-load lever above remains the reclamation path.

Combined-chunks limit raised 3.07->3.08 MB after the terminal clickable-links feature (terminal-links + terminal-link-provider land in the already-lazy TerminalPanel chunk, not the eager bundle — main app stayed at 460.84 kB) plus concurrent main growth on rebase pushed the combined total 315 B over 3.07 MB; the TerminalDock lazy-load lever above remains the reclamation path.

Main app bundle limit raised 462→466 kB after merging main + the tree-view Show visibility feature: eager weight is the Show menu group across the tree-options popover / context menu, the shared visibility predicates, and the NotInSidebarIndicator (~1-2 kB), with the rest concurrent main growth absorbed across three catch-up merges (bundle measured 464.63 kB, 2.63 kB over); the PropertyPanel/TerminalDock/BottomComposer lazy-load levers above remain the reclamation path.

Combined-chunks limit raised 3.08->3.25 MB (measured 3.22 MB after merging main) for the ACP agent-threads feature (in-app threads + unified sessions dock): the thread-client/thread-event-model chain, ThreadView, and the dock UI are lazy chunks (TerminalSessionsHost, AgentBuildShowcase, AgentThreadClientBinder, and the desktop TerminalWindowApp are all React.lazy mounts, so the main bundle stays under its 466 kB limit at 458 kB), but the combined glob counts lazy chunks too, so the feature's total JS footprint lands here.

Main CSS limit raised 52->53 kB — merging the ACP dock/thread surfaces onto main's CSS accretion pushed global utilities to 52.86 kB (863 B over), no single bloat source and no lazy-load lever for global CSS.

Combined-chunks 3.25->3.35 MB and CSS 54->56 kB after merging main (markdownlint content rules, Themes, external-link-preview, in-app Report a bug, GitHub Copilot CLI) into the ACP-threads branch — the merged build measures 3.31 MB / 55 kB combining both feature sets (main app 469 kB, under its 476 kB limit); no single reclaimable source beyond the lazy-load levers above.

Combined-chunks 3.35->3.5 MB and CSS 56->57 kB after merging main's bun->pnpm (Node 24) toolchain migration into the ACP-threads branch — a fresh pnpm resolution floats several bundled deps to newer patches within their existing caret ranges (dep declarations unchanged, no duplicate versions), diffuse drift inherent to the package-manager swap with no single reclaimable source; measured 3.45 MB / 56.38 kB, main app stays 469 kB under its 482 kB limit.

Merged the in-app feedback form onto these post-toolchain limits: main-app 482->485 kB (feedback's only eager weight is the HelpPopover Provide-feedback entry + dialog shell; FeedbackForm and its zod/react-hook-form/attachment deps are lazy behind FeedbackFormDialog), CSS 57->59 kB (the form's globals.css utilities — .scroll-fade-x mask, .scrollbar-none, shimmer @utility/@keyframes — plus Tailwind accretion from FeedbackForm + the attachment card), combined 3.5->3.55 MB (the merged tree stacks main's ACP/toolchain features and the lazy FeedbackForm chunk — measured 3,465,453 B locally, ~3.51 MB projected in CI at the ~1.4% local->CI ratio, over the intermediate 3.5); CI size-limit verifies the true merged numbers.

Windows/Linux desktop port stacked on this: the win/linux-only AppMenubar (and its radix Menubar primitive) is lazy-loaded behind the app-menubar-gate predicate so web + macOS never download its chunk; the residual eager weight is the gate + lazy() glue plus the pty-capability gating in EditorPane/SettingsDialogShell, absorbed under the current limits with no reclaimable source beyond the pre-existing lazy-load levers above.

Combined-chunks limit raised 3.6->3.61 MB for the div/center align-wrapper rendering (div-align promoter + HtmlAlignBlock descriptor/renderer; measured 170 B over the combined budget, main bundle unaffected at 485 kB) — the PropertyPanel/TerminalDock/BottomComposer lazy-load levers above remain the reclamation path.

Merged the skills-marketplace branch (in-place skill model, location menus, folder verbs, MCP location parity) onto these post-toolchain limits; both feature sets stack and CI size-limit verifies the true merged numbers. Combined-chunks 3.61->3.7 MB after two catch-up merges of main into this branch (post-stable v0.41.3 and v0.41.4) stacked on the divergent-same-name-skill rows, the per-location convert verb, and the regrouped folders panel — CI measured 3.65 MB (53.55 kB over 3.6); the main app bundle is unaffected at 377 kB against its 485 kB limit, so the growth is lazy-chunk and concurrent-main weight rather than eager, and the PropertyPanel/TerminalDock/BottomComposer lazy-load levers above remain the reclamation path.

Combined-chunks 3.7->3.72 MB after merging main (in-doc comments: CommentedDocProvider / PropertyCommentButton / the comment queue + margin rail, plus the derived-index `onDocumentSettled` hook) into the skills-marketplace branch. CI measured 4.14 kB over 3.7 MB; both feature sets stack and neither is the single source. The main app bundle is unaffected at 395 kB against its 485 kB limit and CSS at 8.4 kB against 59 kB, so the growth is lazy-chunk weight rather than eager — the PropertyPanel/TerminalDock/BottomComposer lazy-load levers above remain the reclamation path.

Combined-chunks limit raised 3.78->3.95 MB (on the ACP bug-bash quick-wins branch; superseded by the parallel 4.5 MB raise below before reaching main) for ACP syntax highlighting: the in-house streamdown code-highlighter plugin — shiki core + the JavaScript regex engine (no oniguruma wasm) + 15 curated grammars, with the TS family collapsed onto tsx/javascript since the four near-copy grammars cost ~140 kB gz for no visible difference — lands entirely in a lazy chunk loaded on the first fenced code block. Measured 3.91 MB combined; the main app bundle is unaffected at 408 kB against its 487 kB limit. The off-the-shelf @streamdown/code plugin (full-bundle shiki, every grammar) measured 5.57 MB and was replaced by the curated plugin; trimming the grammar list further is the reclamation lever.

Combined-chunks 3.78->4.4 MB when the interface-language preference started activating catalogs. The ten non-English locales become real on-demand chunks the moment `ConfigProvider` asks for one; until then nothing imported the loader map and rolldown dropped it, so the chunks did not exist to be measured. They add 580,340 B gzipped, 58,034 B each — every catalog is full-size regardless of how much of it is translated, because an untranslated entry compiles to its English source string, so the skeleton catalogs already weigh what finished ones will. Measured 4.31 MB. The main app bundle is untouched at 395.62 kB against its 485 kB limit and CSS at 8.36 kB against 59 kB, which is the point of splitting them: a reader downloads one catalog, not eleven. Importing all eleven eagerly would have put ~638 kB gzipped in front of first paint, more than the entire main-bundle budget on its own, so there is no reclamation lever here — the weight is the feature.

Combined-chunks 4.4->4.5 MB once every catalog held real translations rather than English skeletons. Measured 4.41 MB, 6.02 kB over. The entry above predicted this would cost nothing: an untranslated entry compiles to its English source string, so a skeleton already weighs a full catalog, and translating it swaps bytes rather than adding them. That holds for Latin and CJK and does not hold for the Indic and Arabic scripts. Measured per-locale gzipped chunks now span 60,026 B to 78,840 B, where before every one sat at 58,034 B: Devanagari and Bengali are the two largest at ~78 kB, Arabic and Urdu next at ~71 kB, and the Latin and Han catalogs stay near 60-65 kB. Those scripts are 2-3 UTF-8 bytes per character against English's 1, and gzip recovers less of that on an alphabet as large and combining-mark-heavy as Devanagari than it does on Han. The main app bundle is untouched at 395.23 kB against its 485 kB limit and CSS at 8.36 kB against 59 kB, so this is entirely on-demand catalog weight: a reader still downloads one catalog, and the one they download is now the one they can read. No reclamation lever — same as above, the weight is the feature.

Combined-chunks 4.5->4.7 MB merging the ACP-highlighting branch onto the language catalogs: the two lazy features stack — eleven on-demand locale catalogs plus the curated shiki grammar chunk — and measured 4.6 MB locally, ~4.66 MB projected in CI at the ~1.4% local->CI ratio, 104 kB over the 4.5 ceiling that was set before the highlighter existed. The main app bundle and CSS stay under their limits; both features load nothing until used (a catalog on locale switch, the grammar chunk on the first fenced code block). Reclamation levers: trim the grammar list, or the catalog-weight notes above.

Combined-chunks 4.7->4.71 MB for the frontmatter-schema folder picker: the searchable multi-select UI, live glob-match diagnostics, and their translations across all supported locale chunks measured 4,701,170 B in CI, 1,170 B over the prior ceiling. The eager app bundle remains 415.93 kB against 487 kB and CSS remains 8.36 kB against 59 kB; this is a measured 10 kB combined-budget increment for the shipped feature rather than an eager-loading regression.

Combined-chunks 4.7->4.72 MB for uniform local-target classification and image failure states across the editor, Problems, Links, and source diagnostics. CI measured 4.71 MB, 8.53 kB over the prior ceiling, while the main app bundle remained at 418.08 kB against its 487 kB limit and CSS at 8.36 kB against 59 kB. The increase therefore records the feature's total chunk footprint rather than first-load growth; the pre-existing lazy-load levers above remain the reclamation path.

Combined-chunks kept at 4.75 MB when merging main. Both sides had raised the same budget off a 4.7 MB base that had no headroom left: this branch to 4.75 for the gitignored-skill dialog (measured 364 B over, 0.008%), main to 4.72 for the entry above. The merged tree carries both features, so the resolution takes the higher ceiling rather than either side's number. CI measured 4.7 MB against 4.75 on the pre-merge branch head and the post-merge tree measures 4.69 MB locally, where local reads a little under CI. The lazy-load levers above remain the reclamation path.

Combined-chunks 4.75->4.78 MB for the Open Knowledge Format plugin controls, generated-index settings, starter-pack install card, and their translations across all supported locale chunks. CI measured 4.76 MB, 12.84 kB over the prior ceiling, while the main app bundle remained at 417.92 kB against its 487 kB limit and CSS at 8.36 kB against 59 kB. The increase records the feature's total chunk footprint rather than first-load growth; the pre-existing lazy-load levers above remain the reclamation path.

Combined-chunks 4.85->4.95 MB for Korean (`ko`), the twelfth supported locale. Its catalog is emitted as a separate on-demand chunk, so the increase is combined-chunk inventory rather than eager weight: the branch measured 4.86 MB locally, 8.85 kB over the prior ceiling, while the main app remained at 420.63 kB against its 487 kB limit and CSS at 8.36 kB against 59 kB. The new ceiling leaves about 91 kB (1.9%) of headroom for the local-to-CI variance recorded above.

Combined-chunks 4.95->6.55 MB for the `.excalidraw` canvas doc editor (`@excalidraw/excalidraw@^0.18.1`, matched to Excalidraw's own file format via `serializeAsJSON` / `restore`). Weight is entirely on-demand: `ExcalidrawDocEditor` mounts through `EditorActivityPool` behind the existing `React.lazy` gate, and Excalidraw itself defers its scene renderer, mermaid-to-excalidraw importer, and rough.js / perfect-freehand stack into its own async chunks — the eager main bundle is unaffected (measured 421.2 kB against its 487 kB limit). The gzipped total across the added `chunk-EIO257PC-*.js` + `dist-*.js` + `ExcalidrawDocEditor-*.js` chunks measures 6.41 MB locally, ~1.46 MB above the prior 4.95 MB ceiling; 6.55 MB carries the addition with ~140 kB headroom for the local-to-CI variance recorded above. No smaller Excalidraw slice exists on npm (the package ships one entry with the scene renderer, roughjs, perfect-freehand, `@excalidraw/laser-pointer`, and `@excalidraw/mermaid-to-excalidraw` all wired together), and the workspace deliberately vendors the fonts to `packages/app/public/excalidraw-assets/` rather than accepting the upstream `esm.sh` CDN fallback (see `scripts/copy-excalidraw-assets.mjs`) — the vendored assets are static files under `public/`, not JS, so they do not count against this budget.
